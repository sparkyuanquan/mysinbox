name: Build Minimal Sing-Box

on:
  workflow_dispatch: # 支持手动点击运行

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Build sing-box for ARM64
        run: |
          # 1. 克隆 sing-box 最新代码
          git clone https://github.com/SagerNet/sing-box.git
          cd sing-box
          
          # 2. 交叉编译 aarch64 架构，仅保留必要的 openconnect 和 gvisor 模块
          # 输出名字改为 sing-box-bin 避免与目录重名
          GOOS=linux GOARCH=arm64 CGO_ENABLED=0 \
          go build -v \
            -trimpath \
            -ldflags="-s -w -buildid=" \
            -tags "with_gvisor,with_openconnect" \
            -o ../sing-box-bin ./cmd/sing-box

      - name: Compress Binary
        run: |
          # 3. 安装 UPX 并压缩二进制文件
          sudo apt-get update && sudo apt-get install -y upx
          upx --best --lzma sing-box-bin

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: sing-box-arm64
          path: sing-box-bin
